This app was migrated from AWS (including the lambda, which obviously won't work here).

All the audio files and cache ids of the scripts are under tr-freeder (I think)

The whole setup needs fixing for a new environ, if at all.
